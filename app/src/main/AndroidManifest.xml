<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jenshen.smartmirror">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.SmartMirrorApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/App.Theme"
        tools:replace="theme">
        <activity
            android:name=".ui.activity.splash.SplashActivity"
            android:noHistory="true"
            android:theme="@style/App.Theme.Splash.Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="3b1adbe3949cfd0e01ca89038fa1a880df54aa38" />

        <activity
            android:name=".ui.activity.start.StartActivity"
            android:theme="@style/App.Theme.Start.Activity" />
        <activity
            android:name=".ui.activity.signIn.SignInActivity"
            android:theme="@style/App.Theme.SignIn.Activity"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity
            android:name=".ui.activity.signup.SignUpActivity"
            android:theme="@style/App.Theme.SignIn.Activity"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity
            android:name=".ui.activity.dashboard.tuner.TunerActivity"
            android:theme="@style/App.Theme.Tuner.Activity" />
        <activity
            android:name=".ui.activity.settings.SettingsActivity"
            android:theme="@style/App.Theme.Settings.Activity" />
    </application>

</manifest>